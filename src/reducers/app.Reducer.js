// app reducer

const initState = {
  locale: "it",
};

const appReducer = (state = initState, action) => {
  switch (action.payload) {
    default:
      return state;
  }
};

export default appReducer;
